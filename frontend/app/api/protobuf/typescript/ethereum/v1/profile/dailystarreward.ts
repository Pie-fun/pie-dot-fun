// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v1.178.0
//   protoc               unknown
// source: ethereum/v1/profile/dailystarreward.proto

/* eslint-disable */
import _m0 from "protobufjs/minimal";
import { Timestamp } from "../../../google/protobuf/timestamp";

export const protobufPackage = "ethereum.v1.profile";

export interface GetDailyStarRewardRequest {
  /**
   * Format: profiles/{address}
   * Resource name alias "profiles/me" is supported.
   */
  profile: string;
}

export interface ClaimMyDailyStarRewardRequest {
}

export interface DailyStarReward {
  profile: string;
  currentStreak: number;
  isClaimable: boolean;
  starReward: number;
  lastClaim: DailyStarRewardClaim | undefined;
}

export interface DailyStarRewardClaim {
  streak: number;
  claimTime: string | undefined;
}

function createBaseGetDailyStarRewardRequest(): GetDailyStarRewardRequest {
  return { profile: "" };
}

export const GetDailyStarRewardRequest = {
  encode(message: GetDailyStarRewardRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.profile !== "") {
      writer.uint32(10).string(message.profile);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): GetDailyStarRewardRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetDailyStarRewardRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.profile = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetDailyStarRewardRequest {
    return { profile: isSet(object.profile) ? globalThis.String(object.profile) : "" };
  },

  toJSON(message: GetDailyStarRewardRequest): unknown {
    const obj: any = {};
    if (message.profile !== undefined) {
      obj.profile = message.profile;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetDailyStarRewardRequest>, I>>(base?: I): GetDailyStarRewardRequest {
    return GetDailyStarRewardRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetDailyStarRewardRequest>, I>>(object: I): GetDailyStarRewardRequest {
    const message = createBaseGetDailyStarRewardRequest();
    message.profile = object.profile ?? "";
    return message;
  },
};

function createBaseClaimMyDailyStarRewardRequest(): ClaimMyDailyStarRewardRequest {
  return {};
}

export const ClaimMyDailyStarRewardRequest = {
  encode(_: ClaimMyDailyStarRewardRequest, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): ClaimMyDailyStarRewardRequest {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClaimMyDailyStarRewardRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ClaimMyDailyStarRewardRequest {
    return {};
  },

  toJSON(_: ClaimMyDailyStarRewardRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<ClaimMyDailyStarRewardRequest>, I>>(base?: I): ClaimMyDailyStarRewardRequest {
    return ClaimMyDailyStarRewardRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ClaimMyDailyStarRewardRequest>, I>>(_: I): ClaimMyDailyStarRewardRequest {
    const message = createBaseClaimMyDailyStarRewardRequest();
    return message;
  },
};

function createBaseDailyStarReward(): DailyStarReward {
  return { profile: "", currentStreak: 0, isClaimable: false, starReward: 0, lastClaim: undefined };
}

export const DailyStarReward = {
  encode(message: DailyStarReward, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.profile !== "") {
      writer.uint32(10).string(message.profile);
    }
    if (message.currentStreak !== 0) {
      writer.uint32(16).uint32(message.currentStreak);
    }
    if (message.isClaimable !== false) {
      writer.uint32(24).bool(message.isClaimable);
    }
    if (message.starReward !== 0) {
      writer.uint32(32).uint32(message.starReward);
    }
    if (message.lastClaim !== undefined) {
      DailyStarRewardClaim.encode(message.lastClaim, writer.uint32(42).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DailyStarReward {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDailyStarReward();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.profile = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.currentStreak = reader.uint32();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.isClaimable = reader.bool();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.starReward = reader.uint32();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.lastClaim = DailyStarRewardClaim.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DailyStarReward {
    return {
      profile: isSet(object.profile) ? globalThis.String(object.profile) : "",
      currentStreak: isSet(object.currentStreak) ? globalThis.Number(object.currentStreak) : 0,
      isClaimable: isSet(object.isClaimable) ? globalThis.Boolean(object.isClaimable) : false,
      starReward: isSet(object.starReward) ? globalThis.Number(object.starReward) : 0,
      lastClaim: isSet(object.lastClaim) ? DailyStarRewardClaim.fromJSON(object.lastClaim) : undefined,
    };
  },

  toJSON(message: DailyStarReward): unknown {
    const obj: any = {};
    if (message.profile !== undefined) {
      obj.profile = message.profile;
    }
    if (message.currentStreak !== undefined) {
      obj.currentStreak = Math.round(message.currentStreak);
    }
    if (message.isClaimable !== undefined) {
      obj.isClaimable = message.isClaimable;
    }
    if (message.starReward !== undefined) {
      obj.starReward = Math.round(message.starReward);
    }
    if (message.lastClaim !== undefined) {
      obj.lastClaim = DailyStarRewardClaim.toJSON(message.lastClaim);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DailyStarReward>, I>>(base?: I): DailyStarReward {
    return DailyStarReward.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DailyStarReward>, I>>(object: I): DailyStarReward {
    const message = createBaseDailyStarReward();
    message.profile = object.profile ?? "";
    message.currentStreak = object.currentStreak ?? 0;
    message.isClaimable = object.isClaimable ?? false;
    message.starReward = object.starReward ?? 0;
    message.lastClaim = (object.lastClaim !== undefined && object.lastClaim !== null)
      ? DailyStarRewardClaim.fromPartial(object.lastClaim)
      : undefined;
    return message;
  },
};

function createBaseDailyStarRewardClaim(): DailyStarRewardClaim {
  return { streak: 0, claimTime: undefined };
}

export const DailyStarRewardClaim = {
  encode(message: DailyStarRewardClaim, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.streak !== 0) {
      writer.uint32(8).uint32(message.streak);
    }
    if (message.claimTime !== undefined) {
      Timestamp.encode(toTimestamp(message.claimTime), writer.uint32(18).fork()).ldelim();
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): DailyStarRewardClaim {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDailyStarRewardClaim();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.streak = reader.uint32();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.claimTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DailyStarRewardClaim {
    return {
      streak: isSet(object.streak) ? globalThis.Number(object.streak) : 0,
      claimTime: isSet(object.claimTime) ? globalThis.String(object.claimTime) : undefined,
    };
  },

  toJSON(message: DailyStarRewardClaim): unknown {
    const obj: any = {};
    if (message.streak !== undefined) {
      obj.streak = Math.round(message.streak);
    }
    if (message.claimTime !== undefined) {
      obj.claimTime = message.claimTime;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DailyStarRewardClaim>, I>>(base?: I): DailyStarRewardClaim {
    return DailyStarRewardClaim.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DailyStarRewardClaim>, I>>(object: I): DailyStarRewardClaim {
    const message = createBaseDailyStarRewardClaim();
    message.streak = object.streak ?? 0;
    message.claimTime = object.claimTime ?? undefined;
    return message;
  },
};

export interface DailyStarRewardService {
  /** GetDailyStarReward will return the Profile's DailyStarReward */
  GetDailyStarReward(request: GetDailyStarRewardRequest): Promise<DailyStarReward>;
  /** ClaimMyDailyStarReward will claim the Profile's DailyStarReward */
  ClaimMyDailyStarReward(request: ClaimMyDailyStarRewardRequest): Promise<DailyStarReward>;
}

export const DailyStarRewardServiceServiceName = "ethereum.v1.profile.DailyStarRewardService";
export class DailyStarRewardServiceClientImpl implements DailyStarRewardService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || DailyStarRewardServiceServiceName;
    this.rpc = rpc;
    this.GetDailyStarReward = this.GetDailyStarReward.bind(this);
    this.ClaimMyDailyStarReward = this.ClaimMyDailyStarReward.bind(this);
  }
  GetDailyStarReward(request: GetDailyStarRewardRequest): Promise<DailyStarReward> {
    const data = GetDailyStarRewardRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetDailyStarReward", data);
    return promise.then((data) => DailyStarReward.decode(_m0.Reader.create(data)));
  }

  ClaimMyDailyStarReward(request: ClaimMyDailyStarRewardRequest): Promise<DailyStarReward> {
    const data = ClaimMyDailyStarRewardRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ClaimMyDailyStarReward", data);
    return promise.then((data) => DailyStarReward.decode(_m0.Reader.create(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(dateStr: string): Timestamp {
  const date = new globalThis.Date(dateStr);
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): string {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis).toISOString();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
